# The deployed backend's base URL (no trailing slash, no /api suffix).
# Only needed in production builds — local dev always talks to
# http://localhost:7500 regardless of this value.
VITE_API_URL=https://your-backend.onrender.com
